version: "3.9"

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8080
    volumes:
      - .:/code
    ports:
      - "8080:8080"
    links:
      - redis

  redis:
    image: redis:latest
    ports:
      - "6379:6379"